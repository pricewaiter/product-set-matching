{
  "name": "product-set-matching",
  "version": "0.2.0",
  "description": "Evaluates product data against a ruleset of matching criteria.",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint src test",
    "test-mocha": "mocha test",
    "test": "npm run lint && npm run test-mocha"
  },
  "repository": {
    "type": "git",
    "url": "github.com/pricewaiter/product-set-matching"
  },
  "engines": {
    "node": ">= 4.0.0"
  },
  "author": "PriceWaiter",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "eslint": "^3.16.1",
    "eslint-config-airbnb-base": "^11.1.0",
    "eslint-plugin-import": "^2.2.0",
    "mocha": "^3.2.0"
  },
  "dependencies": {
    "debug": "^2.6.1"
  }
}
